#!/usr/bin/env bash

tmppwd=`pwd`
cd `dirname $0`

_cv(){
  local arg_name=$1 
  shift  
  ./libexec/cv-$arg_name "$@"
}

if [[ $# == 0 ]]
then
    _cv usage  | less 
    exit 1
fi


var=$1
if [[ !  -f "./libexec/cv-$var" ]]
then
    _cv usage  | less
    exit 1
fi

_cv "$@"

cd $tmppwd
